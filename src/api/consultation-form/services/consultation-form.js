'use strict';

/**
 * consultation-form service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::consultation-form.consultation-form');
